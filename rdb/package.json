{
  "name": "@ck/ck-rdb",
  "version": "1.0.0",
  "description": "setup rdb",
  "private": true,
  "scripts": {
    "test": "echo No Tests",
    "clean": "rm -rf node_modules",
    "migrate:up": "db-migrate up",
    "migrate:reset": "db-migrate reset",
    "migrate:resetup": "npm run migrate:reset && npm run migrate:up",
    "migrate:up:sample": "db-migrate up:sample",
    "migrate:reset:sample": "db-migrate reset:sample",
    "migrate:resetup:sample": "npm run migrate:reset:sample && npm run migrate:up:sample",
    "migrate:create": "db-migrate create",
    "redis:resetup": "node redis.js"
  },
  "author": "Christian Daga",
  "license": "ISC",
  "dependencies": {
    "db-migrate": "^0.11.13",
    "db-migrate-pg": "^1.2.2",
    "ioredis": "^5.1.0"
  }
}
